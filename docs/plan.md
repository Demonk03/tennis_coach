# План: AI Tennis Coach

Составлен 2026-09-07 по итогам кастдева — см. [custdev-2026-09.md](custdev-2026-09.md).

Порядок важен: P0 дешёвый и бьёт в главную работу, P3 без P2 бессмысленен.

---

## Сделано

- [x] Убрать интеграцию с Oura (код, фронт, тесты, конфиги, документация)

---

## P0. Тактика с поправкой на состояние

Единственная правка — `gpt.py`. Ни схемы, ни UI.

- [x] Переписать `MATCH_PLAN_PROMPT`: `tactics` выводить из `energy_level` + `physical_state` + `mindset` наравне с данными о сопернике
- [x] Явно задать тип советов при низком состоянии: понизить риск, упростить первую подачу, играть с запасом над сеткой
- [x] Сохранить запрет мотивационных формулировок — работа закрывается тактикой-разрешением, а не подбадриванием
- [ ] Прогнать A/B: одна и та же анкета на `gpt-4o-mini` и на модели посильнее, сравнить конкретность
- [ ] Проверить на 2–3 реальных матчах: изменил ли совет поведение на корте

## P1. Экран перехода

Самая живая работа (J2), узкое место — ввод, а не выдача.

- [ ] Заменить `TOPICS` двумя словарями: «что не идёт у меня» и «что делает соперник», оба мультивыбор
- [ ] Наполнить словари языком пользователя: «много своих ошибок», «рискую», «не доношу мяч», «не двигаюсь», «зажался», «не тяну эмоционально» / «режет», «укорачивает», «возвращает всё», «давит с задней», «играет плоско», «выходит к сетке»
- [ ] Оставить необязательное текстовое поле для расширенного комментария
- [ ] Упростить счёт на переходе до одного тапа: веду / ровно / горю + стадия сета
- [ ] Миграция `match_events` под новую структуру наблюдений
- [ ] Переписать таск-промпт `generate_changeover_advice` под два словаря
- [ ] Целевое время заполнения — до 15 секунд, замерить на реальном переходе
- [ ] Обязательно прочитать ответ в первом же матче после переделки — канал ввода чиним, ценность выдачи ещё не подтверждена

## P2. Пост-матчевая анкета

Три сущности вместо двух. Разблокирует J6.

- [ ] Добавить блок «соперник»: стиль (переиспользовать `OPPONENT_STYLE_CHIPS`) → что против него работает → на чём он ошибается
- [ ] Развести свои ошибки и своё эмоциональное состояние — сейчас это два размытых текстовых поля
- [ ] Добавить метрику ценности: «совет что-то изменил на корте?» да/нет
- [ ] Миграция `match_reviews` под новые поля
- [ ] Хранить наблюдения о сопернике так, чтобы их можно было поднять по имени в следующем матче

## P3. Карточка соперника перед матчем

Включать только после того, как P2 накопит данные.

- [ ] Определить порог: сколько матчей нужно, чтобы карточка была не пустой
- [ ] Подтягивать историю по `opponent_name` на экране подготовки
- [ ] Показывать за 10 секунд: стиль, что работало, на чём ошибается
- [ ] Передавать историю соперника в `MATCH_PLAN_PROMPT`

---

## Отложено

- [ ] Применить миграцию `supabase/migrations/20260907_remove_oura.sql` после явного решения, что история больше не нужна — удаляет столбцы `oura_*` вместе с накопленными значениями
- [ ] VPN: приложение работает только через VPN, включается вручную перед каждым заполнением в машине. Узкое место, решение не выбрано
- [ ] J3 (совет на новый сет): не проверен в кастдеве, вероятно частный случай J2 — пересмотреть после P1

## Решено не делать

- Экран статистических паттернов (J5): данных достаточно (13 матчей), но за весь кастдев тема не всплыла ни разу
- Мотивационные формулировки в советах
