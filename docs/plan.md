# План: AI Tennis Coach

Составлен 2026-09-07 по итогам кастдева — см. [custdev-2026-09.md](custdev-2026-09.md).

Порядок важен: P0 дешёвый и бьёт в главную работу, P3 без P2 бессмысленен.

---

## Сделано

- [x] Убрать интеграцию с Oura (код, фронт, тесты, конфиги, документация)

---

## P0. Тактика с поправкой на состояние

Единственная правка — `gpt.py`. Ни схемы, ни UI.

- [x] Переписать `MATCH_PLAN_PROMPT`: `tactics` выводить из `energy_level` + `physical_state` + `mindset` наравне с данными о сопернике
- [x] Явно задать тип советов при низком состоянии: понизить риск, упростить первую подачу, играть с запасом над сеткой
- [x] Сохранить запрет мотивационных формулировок — работа закрывается тактикой-разрешением, а не подбадриванием

## P1. Экран перехода

Самая живая работа (J2), узкое место — ввод, а не выдача.

- [x] Заменить `TOPICS` двумя словарями: «что не идёт у меня» и «что делает соперник», оба мультивыбор
- [x] Наполнить словари языком пользователя: «много своих ошибок», «рискую», «не доношу мяч», «не двигаюсь», «зажался», «не тяну эмоционально» / «режет», «укорачивает», «возвращает всё», «давит с задней», «играет плоско», «выходит к сетке»
- [x] Оставить необязательное текстовое поле для расширенного комментария
- [x] Упростить счёт на переходе до одного тапа: веду / ровно / горю + стадия сета
- [x] Подготовить миграцию `match_events` под новую структуру наблюдений
- [x] Переписать таск-промпт `generate_changeover_advice` под два словаря

## P2. Пост-матчевая анкета

Три сущности вместо двух. Разблокирует J6.

- [x] Добавить блок «соперник»: стиль (переиспользовать `OPPONENT_STYLE_CHIPS`) → что против него работает → на чём он ошибается
- [x] Развести свои ошибки и своё эмоциональное состояние — сейчас это два размытых текстовых поля
- [x] Добавить метрику ценности: «совет что-то изменил на корте?» да/нет
- [x] Подготовить миграцию `match_reviews` под новые поля
- [x] Хранить наблюдения о сопернике так, чтобы их можно было поднять по имени в следующем матче

## P3. Карточка соперника перед матчем

Включать только после того, как P2 накопит данные.

- [x] Определить порог: 1 завершённый матч с разбором и полезными наблюдениями
- [x] Подтягивать историю по `opponent_name` на экране подготовки
- [x] Показывать за 10 секунд: стиль, что работало, на чём ошибается
- [x] Передавать историю соперника в `MATCH_PLAN_PROMPT`

---

## Отложено

- [ ] Полевая проверка P0: A/B `gpt-4o-mini` и более сильной модели, затем 2–3 реальных матча с замером изменения поведения
- [ ] Полевая проверка P1: замерить заполнение до 15 секунд и обязательно прочитать первый ответ после переделки
- [ ] Применить миграцию `supabase/migrations/20260907_remove_oura.sql` после явного решения, что история больше не нужна — удаляет столбцы `oura_*` вместе с накопленными значениями
- [ ] VPN: приложение работает только через VPN, включается вручную перед каждым заполнением в машине. Узкое место, решение не выбрано
- [ ] J3 (совет на новый сет): не проверен в кастдеве, вероятно частный случай J2 — пересмотреть после P1

## Решено не делать

- Экран статистических паттернов (J5): данных достаточно (13 матчей), но за весь кастдев тема не всплыла ни разу
- Мотивационные формулировки в советах
